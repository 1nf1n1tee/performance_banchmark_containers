FROM python:3.11-slim

WORKDIR /app

COPY dedup_utils.py .
COPY crc32_dedup.py .
COPY entry_1mil.csv .
COPY epoch_crc.py .

# Install necessary Python packages
RUN pip install --no-cache-dir psutil

# Run the script
CMD ["python3", "epoch_crc.py"]
